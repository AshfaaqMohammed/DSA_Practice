package AlgorithmAnalysis.sec42;

public class CommonFunctionsToAnalyze {
    /*
        - 1. Constant Function
            f(n) = c
        - 2. Logarithm funciton
            f(n) = log n
        - 3. Liner function
            f(n) = n
        - 4. NlogN function
            f(n) = nlog n
             if we perform an algorithm
            with nested loops such that the operations in the inner loop increase by one each
            time, then the total number of operations is quadratic in the number of times, n,
            we perform the outer loop.
        - 5. quadratic function
            f(n) = n^2
        - 6. cubical function
            f(n) = n^3
        - 7. Exponential Function
            f(n) = b^n
     */
}
